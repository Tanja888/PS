# Read in a number and add one
# Author: Tanja Juric

# number variable has to be converted into integer to do the calculation
number = int(input('Please enter a number: '))
newNumber = number + 1

print ('{} plus one is {}.'.format(number, newNumber))
